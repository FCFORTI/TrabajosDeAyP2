package Guía3Ejercicio4;
import java.util.Comparator;

public class ComparadorDeDocentes implements Comparator<Docentes>{

	@Override
	public int compare(Docentes o1, Docentes o2) {
		if(o1.calcularSueldo() > o2.calcularSueldo()){
			return -1;
		} else if(o1.calcularSueldo() < o2.calcularSueldo()){
			return 1;
		}
		return 0;
	}

}
